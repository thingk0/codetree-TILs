#include <iostream>

using namespace std;

int main() {
    int a, b, c;

    cin >> a;
    cin.get();
    cin >> b;
    cin.get();
    cin >> c;

    cout << b << '-' << c << '-' << a;
    return 0;
}